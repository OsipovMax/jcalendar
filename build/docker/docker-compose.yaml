version: "3"

services:
  app:
    container_name: ${MODE}_app
    build:
      context: ../..
      dockerfile: ./build/docker/Dockerfile
      args:
        - APP_PATH=${APP_PATH}
    command: make run
    depends_on:
#      - redis
#      - kafka
      - postgres
    links:
#      - redis:redis.local
#      - kafka:kafka.local
      - postgres:postgres.local
    working_dir: ${APP_PATH}
    volumes:
      - ../..:${APP_PATH}
      - $GOPATH/pkg/mod:/go/pkg/mod

  postgres:
    container_name: ${MODE}_postgres
    image: ${POSTGRES_VERSION}
    ports:
      - "${POSTGRES_PORT}"
    expose:
      - "${POSTGRES_PORT}"
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data

#  redis:
#    container_name: ${MODE}_redis
#    image: ${REDIS_VERSION}
#    ports:
#      - "6379"
#    expose:
#      - "6379"
#    volumes:
#      - redis-data:/data
#      - redis-dump:/var/lib/redis

#  kafka:
#    container_name: ${MODE}_kafka
#    image: wurstmeister/kafka
#    hostname: kafka
#    ports:
#      - "9092"
#    environment:
#      KAFKA_BROKER_ID: 1
#      KAFKA_ADVERTISED_LISTENERS: INSIDE://kafka:19092,OUTSIDE://${DOCKER_HOST_IP:-127.0.0.1}:9092
#      KAFKA_LISTENERS: INSIDE://:19092,OUTSIDE://:9092
#      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INSIDE:PLAINTEXT,OUTSIDE:PLAINTEXT
#      KAFKA_INTER_BROKER_LISTENER_NAME: INSIDE
#      KAFKA_ADVERTISED_PORT: 9092
#      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"
#      KAFKA_NUM_PARTITIONS: 1
#      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
#      KAFKA_OFFSETS_TOPIC_NUM_PARTITIONS: 1
#    volumes:
#      - /var/run/docker.sock:/var/run/docker.sock
#      - kafka-data:/opt/kafka
#    depends_on:
#      - zookeeper
#
#  zookeeper:
#    container_name: ${MODE}_zookeeper
#    image: zookeeper:3.4.14
#    ports:
#      - "2181"

#  jaeger:
#    container_name: ${MODE}_jaeger
#    image: jaegertracing/all-in-one:1.22
#    environment:
#      COLLECTOR_ZIPKIN_HTTP_PORT: 9411
#    ports:
#      - "5775:5775/udp"
#      - "6831:6831/udp"
#      - "6832:6832/udp"
#      - "5778:5778"
#      - "16686:16686"
#      - "14268:14268"
#      - "9411:9411"

volumes:
  redis-data:
    driver: local
  redis-dump:
    driver: local
  postgres-data:
    driver: local
  kafka-data:
    driver: local
  hoverfly-data:
    driver: local

networks:
  default:
    name: ${APP_PROJECT}
